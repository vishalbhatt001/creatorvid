# Postgres connection string used by Prisma.
# When running everything via docker-compose the host is "postgres".
# For local (non-docker) development against the compose Postgres use "localhost".
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/video_arena?schema=public"
