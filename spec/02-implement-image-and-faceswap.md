## Features
Build on top of the current app. We already have a video feature. Lets add 
1. image generation features (again use openrouter for routing to an image model).
2. Face swap feature. A user can come, put a base image and a face to replace and we should do it for them. Use the locally running FaceFusion self hosted service for this.

Keep the endpoints/code re-usable as much as possible.
